{
  "name": "npm-book-framework",
  "version": "1.0.0",
  "description": "a framework based on artus.js",
  "main": "lib",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "cd example/app && NODE_ENV=dev ARTUS_SERVER_ENV=dev npm start"
  },
  "workspaces": [
    "packages/*"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.6.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@artus/core": "^2.1.1",
    "reflect-metadata": "^0.1.13"
  }
}